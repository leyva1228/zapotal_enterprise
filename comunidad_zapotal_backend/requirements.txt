# === Zapotal Enterprise - Dependencias de producción ===
# Solo paquetes usados directamente por el código, en INSTALLED_APPS,
# middleware, configuración (settings.py) o modelos.
# Las dependencias transitivas se resuelven automáticamente con pip.

# === Core ===
Django==6.0.4

# === API REST ===
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
django-filter==25.2
django-cors-headers==4.9.0
drf-spectacular==0.29.0

# === Storage / Files (Cloudflare R2) ===
django-storages[s3]==1.14.6
boto3==1.43.34

# === Email (Resend) ===
django-anymail[resend]==15.0

# === Static files (whiteNoise para producción) ===
whitenoise==6.12.0

# === Database (MySQL) ===
mysqlclient==2.2.8

# === Image handling (ImageField) ===
pillow==12.2.0

# === Config / settings ===
python-decouple==3.8

# === 2FA (TOTP) ===
pyotp==2.10.0
qrcode==8.2

# === HTTP client (servicios externos) ===
requests==2.33.1

# === Pasarela de pagos (Mercado Pago Checkout Bricks) ===
mercadopago==3.2.0

# === Generacion de PDFs (boletas de donacion) ===
xhtml2pdf==0.2.17
