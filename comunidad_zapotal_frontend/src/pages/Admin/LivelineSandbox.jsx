import React, { useState, useEffect } from "react";
import { Liveline } from "liveline";
/** * SANDBOX - Pruebas reales con la libreria Liveline. * Simula los 3 casos de uso del panel admin del proyecto. */// === SIMULADOR 1: Donaciones (AdminDonaciones) ===function DonacionesChart() {  const [data, setData] = useState(null); // null = loading  const [value, setValue] = useState(0);  useEffect(() => {    // Genera datos simulados: donaciones en las ultimas 24h    // agrupadas por hora, monto total por bucket.    const ahora = Math.floor(Date.now() / 1000);    const buckets = [];    for (let h = 24; h >= 0; h--) {      const time = ahora - h * 3600;      // Simula un pico entre las 14-18h.      const horaDelDia = new Date(time * 1000).getHours();      const pico = (horaDelDia >= 14 && horaDelDia <= 18) ? 3 : 1;      const base = Math.random() * 200 + 50;      const value = Math.round(base * pico);      buckets.push({ time, value });    }    setData(buckets);    setValue(buckets[buckets.length - 1].value);  }, []);  if (!data) return <div style={{ height: 180, background: "white", borderRadius: 12, display: "flex", alignItems: "center", justifyContent: "center", color: "#9ca3af" }}>Cargando datos...</div>;  return (    <div style={{ background: "white", borderRadius: 12, padding: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.08)" }}>      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }}>        <h3 style={{ margin: 0, fontSize: 14, fontWeight: 700, color: "#14532d" }}>          Monto recaudado (últimas 24h)        </h3>        <span style={{ fontSize: 18, fontWeight: 700, color: "#16a34a" }}>          S/ {value.toFixed(2)}        </span>      </div>      <div style={{ height: 180 }}>        <Liveline          data={data}          value={value}          color="#16a34a"          theme="light"          grid          scrub          fill          badge          window={86400}          windows={[            { label: "1h", secs: 3600 },            { label: "6h", secs: 21600 },            { label: "24h", secs: 86400 },          ]}          formatValue={(v) => `S/ ${v.toFixed(2)}`}        />      </div>    </div>  );}// === SIMULADOR 2: Registros de usuarios (AdminDashboard) ===function UsuariosChart() {  const [data, setData] = useState(null);  const [value, setValue] = useState(0);  useEffect(() => {    const ahora = Math.floor(Date.now() / 1000);    const buckets = [];    for (let d = 7; d >= 0; d--) {      const time = ahora - d * 86400;      const value = Math.floor(Math.random() * 8 + 2);      buckets.push({ time, value });    }    setData(buckets);    setValue(buckets[buckets.length - 1].value);  }, []);  if (!data) return <div style={{ height: 140, background: "white", borderRadius: 12, display: "flex", alignItems: "center", justifyContent: "center", color: "#9ca3af" }}>Cargando...</div>;  return (    <div style={{ background: "white", borderRadius: 12, padding: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.08)" }}>      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }}>        <h3 style={{ margin: 0, fontSize: 14, fontWeight: 700, color: "#14532d" }}>          Registros (últimos 7 días)        </h3>        <span style={{ fontSize: 18, fontWeight: 700, color: "#3b82f6" }}>          {value} usuarios        </span>      </div>      <div style={{ height: 140 }}>        <Liveline          data={data}          value={value}          color="#3b82f6"          theme="light"          grid          scrub          fill          window={604800}          windows={[            { label: "24h", secs: 86400 },            { label: "7d", secs: 604800 },          ]}          formatValue={(v) => `${Math.round(v)} usuarios`}        />      </div>    </div>  );}// === SIMULADOR 3: Actividad de auditoría multi-series (AdminAuditoria) ===function AuditoriaChart() {  const [series, setSeries] = useState(null);  useEffect(() => {    const ahora = Math.floor(Date.now() / 1000);    const ACCIONES = [      { id: "login", color: "#3b82f6", label: "LOGIN" },      { id: "create", color: "#16a34a", label: "CREATE" },      { id: "delete", color: "#ef4444", label: "DELETE" },    ];    const result = ACCIONES.map((acc) => {      const data = [];      for (let m = 60; m >= 0; m--) {        const time = ahora - m * 60;        const value = acc.id === "login"          ? Math.floor(Math.random() * 8 + 1)          : Math.floor(Math.random() * 3);        data.push({ time, value });      }      return { id: acc.id, data, value: data[data.length - 1].value, color: acc.color, label: acc.label };    });    setSeries(result);  }, []);  if (!series) return <div style={{ height: 180, background: "white", borderRadius: 12, display: "flex", alignItems: "center", justifyContent: "center", color: "#9ca3af" }}>Cargando...</div>;  return (    <div style={{ background: "white", borderRadius: 12, padding: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.08)" }}>      <h3 style={{ margin: "0 0 8px", fontSize: 14, fontWeight: 700, color: "#14532d" }}>        Actividad del sistema (última hora) — multi-series      </h3>      <div style={{ height: 180 }}>        <Liveline          data={[]}          value={0}          series={series}          color="#3b82f6"          theme="light"          grid          scrub          windowStyle="rounded"          window={3600}          windows={[            { label: "5m", secs: 300 },            { label: "15m", secs: 900 },            { label: "1h", secs: 3600 },          ]}          formatValue={(v) => `${Math.round(v)}`}        />      </div>    </div>  );}export default function LivelineSandbox() {  return (    <div style={{ padding: 24, background: "#f4f6f8", minHeight: "100vh", fontFamily: "Inter, system-ui, sans-serif" }}>      <div style={{ maxWidth: 1200, margin: "0 auto" }}>        <h1 style={{ fontSize: 24, fontWeight: 700, color: "#14532d", marginBottom: 8 }}>          Liveline Sandbox - Datos del proyecto Zapotal        </h1>        <p style={{ color: "#6b7280", marginBottom: 24, fontSize: 14 }}>          Validacion real de la libreria con datos simulados de los 3 lugares prioritarios del panel admin.        </p>        <div style={{ display: "grid", gridTemplateColumns: "1fr", gap: 16, marginBottom: 16 }}>          <DonacionesChart />        </div>        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 16 }}>          <UsuariosChart />          <div style={{ background: "white", borderRadius: 12, padding: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.08)" }}>            <h3 style={{ margin: "0 0 8px", fontSize: 14, fontWeight: 700, color: "#14532d" }}>              Eventos creados (últimos 7 días)            </h3>            <div style={{ height: 140 }}>              <Liveline                data={(() => {                  const ahora = Math.floor(Date.now() / 1000);                  return Array.from({ length: 8 }, (_, i) => ({                    time: ahora - (7 - i) * 86400,                    value: Math.floor(Math.random() * 5),                  }));                })()}                value={2}                color="#a17e4f"                theme="light"                grid                fill                formatValue={(v) => `${Math.round(v)} eventos`}              />            </div>          </div>        </div>        <div style={{ marginTop: 16 }}>          <AuditoriaChart />        </div>        <div style={{ marginTop: 32, padding: 16, background: "white", borderRadius: 8, fontSize: 12, color: "#6b7280" }}>          <strong>Notas técnicas:</strong> Los 3 casos funcionan con la API real de Liveline v0.0.7.          Multi-series requiere ≥ 2 series (en este caso 3: LOGIN, CREATE, DELETE).          El badge flotante solo aparece en modo single-line, no en multi-series.        </div>      </div>    </div>  );}